extends layout

block content
	section#main
		if user
			.centred
				img.round(src="#{user.picture}", width="200") 
				p= user.displayName
				p= user.email
				p= user._raw
				each role in user.roles
					p= role